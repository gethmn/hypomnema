# conflicted draft

Sync-conflict file used to verify that the watcher's
`*.sync-conflict-*` ignore pattern fires for this vault as well as
for `sample-vault/`. Never indexed; never appears in any search.
