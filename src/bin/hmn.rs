fn main() -> anyhow::Result<()> {
    // Step 1 (skeleton) will fill this in: config loading, logging,
    // and graceful SIGINT handling.
    println!("hmn: placeholder. See AGENTS.md for the v0 step plan.");
    Ok(())
}
