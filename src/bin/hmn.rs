fn main() -> anyhow::Result<()> {
    // Step 1 (skeleton) will fill this in: clap dispatch for client
    // subcommands (search, status) that speak HTTP to a running hmnd.
    println!("hmn: placeholder. See AGENTS.md for the v0 step plan.");
    Ok(())
}
